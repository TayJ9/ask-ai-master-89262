# Railpack configuration for Railway
# This tells Railway how to build the project

[build]
# Build command - only install dependencies, no code execution
command = "npm install"

# Start command - runs at runtime
[start]
command = "npm start"

