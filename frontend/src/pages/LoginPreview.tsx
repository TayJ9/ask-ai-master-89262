import LoginScreen from "@/components/LoginScreen";

/** Same split-screen login as production; kept at /login-preview for direct access. */
export default function LoginPreview() {
  return <LoginScreen />;
}
