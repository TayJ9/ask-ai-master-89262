# Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git - it contains sensitive secrets

# ============================================
# BACKEND CONFIGURATION (Railway)
# ============================================

# OpenAI API Key (REQUIRED for voice interviews and AI features)
# Get your API key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# JWT Secret (REQUIRED for authentication)
# Generate: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# Database URL (REQUIRED - Auto-generated by Railway PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Server Port (Optional - Railway sets this automatically)
PORT=8080

# Node Environment
NODE_ENV=production

# Frontend URL (Optional - CORS handles *.vercel.app automatically)
FRONTEND_URL=https://your-frontend-url.vercel.app

# ============================================
# FRONTEND CONFIGURATION (Vercel)
# ============================================

# Backend API URL (REQUIRED for frontend)
VITE_API_URL=https://your-backend-url.up.railway.app

# Supabase Configuration (if using Supabase)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key_here
VITE_SUPABASE_PROJECT_ID=your_supabase_project_id_here
